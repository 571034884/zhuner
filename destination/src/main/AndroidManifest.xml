<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aibabel.traveladvisory">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 友盟 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <meta-data
        android:name="CHANNEL"
        android:value="${CHANNEL_VALUE}" />

    <application
        android:name=".app.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.SearchPageActivity" />
        <activity android:name=".activity.HtmlActivity" />
        <activity android:name=".activity.AllCitysActivity" />
        <activity android:name=".activity.InterestPointActivity" />
        <activity android:name=".activity.WorldCountryActivity" />
        <activity android:name=".activity.CountryActivity" />
        <activity android:name=".activity.CityActivity" />
        <activity
            android:name=".activity.CityDetailsActivity"
            android:exported="true" />
        <activity android:name=".activity.GailanActivity" />
        <activity android:name=".activity.RujingkaActivity"></activity>
    </application>

</manifest>