<?xml version="1.0" encoding="utf-8"?>
<shape
    xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 内部颜色 -->
    <solid
        android:color="#ffffff" />
    <!-- 边缘线条颜色 -->
    <stroke
        android:width="1px"
        android:color="@color/clr_999" />
    <!-- 圆角的幅度 -->
    <corners
        android:topLeftRadius="@dimen/y5"
        android:topRightRadius="@dimen/y5"
        android:bottomLeftRadius="@dimen/y5"
        android:bottomRightRadius="@dimen/y5" />

</shape>
