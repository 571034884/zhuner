<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 内部颜色 -->
    <solid
        android:color="@color/gray1a" />
    <!-- 边缘线条颜色 -->
    <stroke
        android:width="@dimen/px1"
        android:color="@color/gray1a" />
    <!-- 圆角的幅度 -->
    <corners
        android:radius="@dimen/px40" />
</shape>