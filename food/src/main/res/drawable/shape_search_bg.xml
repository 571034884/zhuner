<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 内部颜色 -->
    <solid android:color="@color/while80" />
    <!-- 边缘线条颜色 -->
    <stroke
        android:width="@dimen/y1"
        android:color="@color/white" />
    <!-- 圆角的幅度 -->
    <corners
        android:bottomLeftRadius="@dimen/y28"
        android:bottomRightRadius="@dimen/y28"
        android:topLeftRadius="@dimen/y28"
        android:topRightRadius="@dimen/y28" />

</shape>